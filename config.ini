# /opt/miapp/Autosys/oc-envs.ini
# Líneas que comienzan con # son comentarios.

# =====================================================================
#  GLOBALES (puedes sobreescribir en cada ambiente)
# =====================================================================
[settings]
# Tamaño de bloque al subir pods (N por bloque)
BLOCK_SIZE=4
# Pausa en segundos entre bloques
BLOCK_DELAY=120
# Tiempo máx. para que cada deployment quede en 0 réplicas
TIMEOUT_DOWN=180
# Tiempo máx. de rollout al subir (por deployment)
TIMEOUT_UP=600
# Intervalo de sondeo (segundos) para checar estado
SLEEP_INT=3

# Modo de reinicio:
#  - simple   : scale 0 -> 1 y esperar rollout
#  - redeploy : oc rollout restart + scale 1 + esperar rollout
ROLLOUT_MODE=simple

# Logging
LOG_DIR=/var/log/miapp
# INFO o DEBUG
LOG_VERBOSITY=INFO
# true/false: si falla, adjuntar oc describe + últimos events
COLLECT_DIAG=true

# (Opcional) Namespace por defecto para todos los ambientes.
# Puedes sobreescribirlo por ambiente o en [pods].
# NAMESPACE=mi-namespace-global

# =====================================================================
#  AMBIENTES (OBLIGATORIO definir OC_SERVER, USERNAME, PASSWORD)
#  sobreescribir cualquiera de las claves de [settings] aquí.
#  Si lo haces, el valor del ambiente prevalece sobre [settings].
# =====================================================================
[dev]
OC_SERVER=https://api.ocp-dev:6443
USERNAME=svc_ocp_nonprod
# PASSWORD: pega el valor final (encriptado/codificado) que usas para login
PASSWORD=TU_PASSWORD_DEV
# Overrides opcionales (descomenta si quieres cambiar sólo en dev):
# BLOCK_SIZE=4
# BLOCK_DELAY=60
# TIMEOUT_DOWN=180
# TIMEOUT_UP=600
# SLEEP_INT=3
# ROLLOUT_MODE=simple
# LOG_VERBOSITY=DEBUG
# COLLECT_DIAG=true
# NAMESPACE=miapp-dev

[sit]
OC_SERVER=https://api.ocp-sit:6443
USERNAME=svc_ocp_nonprod
PASSWORD=TU_PASSWORD_SIT
# Overrides opcionales (descomenta si quieres cambiar sólo en dev):
# BLOCK_SIZE=4
# BLOCK_DELAY=60
# TIMEOUT_DOWN=180
# TIMEOUT_UP=600
# SLEEP_INT=3
# ROLLOUT_MODE=simple
# LOG_VERBOSITY=DEBUG
# COLLECT_DIAG=true
# NAMESPACE=miapp-dev

[uat]
OC_SERVER=https://api.ocp-uat:6443
USERNAME=svc_ocp_nonprod
PASSWORD=TU_PASSWORD_UAT
# Overrides opcionales (descomenta si quieres cambiar sólo en dev):
# BLOCK_SIZE=4
# BLOCK_DELAY=60
# TIMEOUT_DOWN=180
# TIMEOUT_UP=600
# SLEEP_INT=3
# ROLLOUT_MODE=simple
# LOG_VERBOSITY=DEBUG
# COLLECT_DIAG=true
# NAMESPACE=miapp-dev

[prod]
OC_SERVER=https://api.ocp-prod:6443
USERNAME=svc_ocp_prod
PASSWORD=TU_PASSWORD_PROD
# Overrides opcionales (descomenta si quieres cambiar sólo en dev):
# BLOCK_SIZE=4
# BLOCK_DELAY=60
# TIMEOUT_DOWN=180
# TIMEOUT_UP=600
# SLEEP_INT=3
# ROLLOUT_MODE=simple
# LOG_VERBOSITY=DEBUG
# COLLECT_DIAG=true
# NAMESPACE=miapp-dev

[cob]
OC_SERVER=https://api.ocp-cob:6443
USERNAME=svc_ocp_prod
PASSWORD=TU_PASSWORD_COB
# Overrides opcionales (descomenta si quieres cambiar sólo en dev):
# BLOCK_SIZE=4
# BLOCK_DELAY=60
# TIMEOUT_DOWN=180
# TIMEOUT_UP=600
# SLEEP_INT=3
# ROLLOUT_MODE=simple
# LOG_VERBOSITY=DEBUG
# COLLECT_DIAG=true
# NAMESPACE=miapp-dev

# =========================================================
#  LISTA COMÚN DE DEPLOYMENTS (ORDEN IMPORTA)
#  Formato: recursos estilo "deploy/<nombre>"
#  Esta lista se usa para TODOS los ambientes.
#  (Opcional) NAMESPACE aquí sirve de Fallback si no está en [settings] ni en el ambiente.
# =========================================================
[pods]
DEPLOYMENTS=deploy/api,deploy/web,deploy/payments,deploy/inventory,deploy/billing,deploy/notifications
# NAMESPACE=mi-namespace-comun
