find /ruta/al/directorio -type d -exec ls -ld {} \; > estructura_directorios.txt


# Dockerfile

# Crear los directorios necesarios en la ruta compartida
RUN mkdir -p /opt/miapp/tmp /opt/miapp/properties /opt/miapp/logback /opt/miapp/certs

# Copiar el archivo .tar y el script de configuración en la ruta compartida de la NAS
COPY configuracion.tar /opt/miapp/tmp/
COPY ajusta_configuracion.sh /opt/miapp/tmp/

# Dar permisos de ejecución al script
RUN chmod +x /opt/miapp/tmp/ajusta_configuracion.sh

